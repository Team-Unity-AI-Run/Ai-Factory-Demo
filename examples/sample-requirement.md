# Sample Requirement — CI/CD Pipeline for FastAPI

**Title:** Generate CI/CD Pipeline

**Body:**

Create a CI/CD pipeline for a Python FastAPI application.

Requirements:

- Install Python dependencies.
- Run unit tests using pytest.
- Run a security scan.
- Build a Docker image.
- Scan the Docker image.
- Deploy to Kubernetes.
- Use GitHub Actions.
- Do not expose secrets in the workflow.

---

Paste the title/body above into a new GitHub Issue in this repository to trigger
the `AI Requirement Processor` workflow (`.github/workflows/ai-requirement.yml`).
